1️⃣ Калькулятор: «Сложи, если сможешь»
🎯 Задача
Создать программу, которая:
Спрашивает два числа и операцию
Выполняет вычисление
Обрабатывает ошибки (не число, деление на ноль)

✅ Шаг 1: Функция для вычислений
python

def calculate(num1, num2, operation):
    if operation == "+":
        return num1 + num2
    elif operation == "-":
        return num1 - num2
    elif operation == "*":
        return num1 * num2
    elif operation == "/":
        if num2 == 0:
            raise ZeroDivisionError("На ноль делить нельзя!")
        return num1 / num2
    else:
        raise ValueError("Неверная операция. Доступно: +, -, *, /")

✅ Шаг 2: Ввод и обработка
python

def main():
    print("🔢 Простой калькулятор")
    print("Доступные операции: +, -, *, /")
    
    try:
        a = float(input("Первое число: "))
        b = float(input("Второе число: "))
        op = input("Операция: ")
        
        result = calculate(a, b, op)
        print(f"Результат: {result}")
        
    except ValueError as e:
        if "could not convert" in str(e):
            print("❗ Ошибка: введите число!")
        else:
            print(f"❗ {e}")
    except ZeroDivisionError as e:
        print(f"❗ Ошибка: {e}")
    except Exception as e:
        print(f"❗ Непредвиденная ошибка: {e}")

✅ Запуск
python

if __name__ == "__main__":
    main()

💡 if __name__ == "__main__": — запускает main() только при прямом запуске файла, а не при импорте

​
Как работает:
Функция calculate принимает два числа и символ операции, возвращает результат.
Проверка операции: только +, -, *, /. Остальные вызывают ValueError.
Деление на ноль — исключение ZeroDivisionError с понятным сообщением.
Функция main:
Запрашивает у пользователя данные.
Конвертирует ввод в float — если не получается, ловит ValueError и сообщает, что нужно ввести число.
Выводит результат или ошибку.